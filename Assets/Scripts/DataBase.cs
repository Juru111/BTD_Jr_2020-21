using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DataBase
{
    //raczej zmieniać Speed tylko przy redBloon

    public static float redSpeed = 1;

    public static float blueSpeed = redSpeed * 1.4f;

    public static float greenSpeed = redSpeed * 1.8f;

    public static float yellowSpeed = redSpeed * 3.2f;

    public static float pinkSpeed = redSpeed * 3.5f;

    public static float blackSpeed = redSpeed * 1.8f;

    public static float whithSpeed = redSpeed * 2;

    public static float purpleSpeed = redSpeed * 3;

    public static float leadSpeed = redSpeed * 1;

    public static float zebraSpeed = redSpeed * 1.8f;

    public static float ranibowSpeed = redSpeed * 2.2f;

    public static float ceramicSpeed = redSpeed * 2.5f;

    public static float MOABSpeed = redSpeed * 1;

}